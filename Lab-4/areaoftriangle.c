#include<stdio.h>

	double main(){
		double B,H,area;
		printf("Enter a value of Base");
		scanf("%lf",&B);
		printf("Enter a value of Height");
		scanf("%lf",&H);
		area=0.5*B*H;
		printf("%f",&area);
		return 0.0;
	}